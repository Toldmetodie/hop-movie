import React from "react";

type SearchBarForSearchPageProps = {
  isMobile: boolean;
};
export const SearchBarForSearchPage = ({
  isMobile,
}: SearchBarForSearchPageProps) => {
  console.log(isMobile);
  return <div>SearchBarForSearchPage</div>;
};
